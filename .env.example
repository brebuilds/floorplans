# OpenAI Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify which OpenAI model to use
# Options: gpt-4o, gpt-4-turbo, gpt-4-vision-preview
# Default: gpt-4o
OPENAI_MODEL=gpt-4o

# Alternative: Use Anthropic Claude (if you prefer)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Note: For production, use environment variables set in your hosting platform
# (Vercel, Netlify, etc.) and never commit .env files with real keys
